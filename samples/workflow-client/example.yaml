name: hello-world
description: A simple demo workflow

steps:
  - name: greet
    type: activity
    activity:
      type: greet
      args:
        name: "World"
      ref: greeting

  - name: log-greeting
    type: activity
    activity:
      type: logNotification
      args:
        message: "Greeting sent: ${greeting.message}"
        level: "info"
