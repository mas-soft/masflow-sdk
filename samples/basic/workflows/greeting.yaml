name: greeting-workflow
description: Simple workflow that greets a user

variables:
  name: "World"

steps:
  - name: say-hello
    type: activity
    activity:
      type: greet
      task_queue: greeter-task-queue
      args:
        name: "${name}"
      ref: greeting
